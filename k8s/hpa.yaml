apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: flood-risk-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: flood-risk-stable
  minReplicas: 3
  maxReplicas: 50
